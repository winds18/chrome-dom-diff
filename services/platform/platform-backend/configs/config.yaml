# 艹，platform-backend配置文件
# 老王精心配置的默认值，生产环境记得改！

debug: true

server:
  port: 8080
  read_timeout: 60
  write_timeout: 60

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  database: platform_db
  sslmode: disable
  max_open_conns: 100
  max_idle_conns: 10
  max_lifetime: 3600  # 秒

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0
  pool_size: 10

jwt:
  secret: "oldwang-super-secret-key-change-me-in-production"
  expire_time: 86400  # 秒 (24小时)

log_level: "info"  # debug, info, warn, error
